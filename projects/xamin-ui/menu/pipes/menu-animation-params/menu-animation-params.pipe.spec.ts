import { MenuAnimationParamsPipe } from './menu-animation-params.pipe';

describe('MenuToggleParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuAnimationParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
