import { IsSubMenuDirective } from './is-sub-menu.directive';

describe('ClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new IsSubMenuDirective();
    expect(directive).toBeTruthy();
  });
});
