import { AnimateDialogDirective } from './animate-dialog.directive';

describe('AnimateDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateDialogDirective();
    expect(directive).toBeTruthy();
  });
});
