import { TemplateDirective } from './template.directive';

describe('MTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateDirective();
    expect(directive).toBeTruthy();
  });
});
