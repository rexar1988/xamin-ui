import { DialogPositionDirective } from './dialog-position.directive';

describe('DialogPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogPositionDirective();
    expect(directive).toBeTruthy();
  });
});
